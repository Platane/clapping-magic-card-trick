{
    "name"              : "js1k-2017",
    "version"           : "0.1.0",

    "dependencies"      : {},

    "devDependencies"   : {},

    "scripts"           : {
        "build"             : "rm -rdf dist/ && mkdir -p dist && cp src/* dist"
    }
}
